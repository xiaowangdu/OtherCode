#include <iostream>
#include <vector>

using namespace std;

int attacks(int hp, int nattack, int battack){
    if(hp <= 0){
        return 0;
    }
    
    vector<int> minattacks(hp + 1, 0);
    minattacks[0] = 0;
    
    for(int i = 1; minattacks.size(); i++){
        
    }
}

int main(){
    int HP, nattack, battack;
    cin >> HP;
    cin >> nattack;
    cin >> battack;
    
    cout << maxvalues(V, volums, values) << endl;
    
    return 0;
}